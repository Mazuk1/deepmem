# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
httpx>=0.27.0

# Config (auto-load .env)
python-dotenv>=1.0.0

# Vector Store
qdrant-client>=1.9.0

# LLM & Embedding
openai>=1.0.0
anthropic>=0.40.0
sentence-transformers>=3.0.0
transformers>=4.40.0
# torch: install separately; for GPU use https://download.pytorch.org/whl/cu121
torch>=2.1.0

# Google Gemini embedding (default embedder backend)
google-genai>=1.0.0

# NLP
numpy>=1.26.0
rank_bm25>=0.2.2

# MCP Server (pin <2.0: FastMCP lives at mcp.server.fastmcp in 1.x; 2.0 moved it)
mcp>=1.0.0,<2.0
